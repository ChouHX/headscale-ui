services:
  headscale-ui:
    build:
      context: .
      dockerfile: Dockerfile
    image: headscale-ui:local
    container_name: headscale-ui
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
    restart: unless-stopped
